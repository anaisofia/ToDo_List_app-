<h2>Editar Tarea</h2>

<%= form_with model: @todos, local: true do |f| %>
  <%= f.label :descripcion %>:
  <%= f.text_area :description %><br />

  <%= f.label :completado? %>:
  <%= f.check_box :completed %><br />

  <%= f.submit "Enviar" %>
<% end %>
